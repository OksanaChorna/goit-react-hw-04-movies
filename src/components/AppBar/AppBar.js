import Container from '../Container';
import Navigation from '../Navigation/Navigation';

const AppBar = () => {
  return (
    <header>
      <Container>
        <Navigation />
      </Container>
    </header>
  );
};

export default AppBar;
